#include <stdio.h>
#include <stdlib.h>

/*
Escreva um programa em C que funcione como uma calculadora. O programa deve apresentar um menu ao usuário da
seguinte forma:
1 – Somar
2 – Subtrair
3 – Multiplicar
4 – Dividir
0 – Sair
Uma estrutura do tipo switch deve ser utilizada para realizar cada operação em um case. Após a escolha da operação,
dois valores devem ser pedidos ao usuário para realizar a operação escolhida. Se a operação escolhida for a 4 o 
dividendo não pode ser zero, um novo valor deve ser solicitado. O programa deve funcionar até que o usuário escolha
a opção 0 (opção de saída).
*/

int main()
{
    int opcao;
    float num1, num2;
    
    do{
        printf("\t\nEscolha uma opçao:\n 1 - SOMAR\n 2 - SUBTRAIR\n 3 - MULTIPLICAR\n 4 - DIVIDIR\n 0 - SAIR\n");
        scanf("%d", &opcao);
        
        switch(opcao){
            case 0:
                printf("\tSAIR!");
                break;
            case 1:
                printf("\tSOMAR\nDigite dois valores: ");
                scanf(" %f %f", &num1,&num2);
                printf("\nResultado: %.2f\n", num1+num2);
                break;
            case 2:
                printf("\tSUBTRAIR\nDigite dois valores: ");
                scanf(" %f %f", &num1,&num2);
                printf("\nResultado: %.2f\n", num1-num2);
                break;
            case 3:
                printf("\tMULTIPLICAR\nDigite dois valores: ");
                scanf(" %f %f", &num1,&num2);
                printf("\nResultado: %.2f\n", num1*num2);
                break;
            case 4:
                    printf("\tDIVIDIR\nDigite dois valores: ");
                    scanf(" %f %f", &num1,&num2);
                    while(num2 == 0){
                        printf("\nDividendo deve ser diferente de ZERO (0). Digite novos valores: ");
                        scanf(" %f %f", &num1,&num2);
                    }
                    printf("\nResultado: %.2f\n", num1/num2);
                    break;
            default:
                printf("\nOPÇÃO INVÁLIDA!");
                break;
        }   
    }
    while(opcao != 0);
    
    return 0;
}
